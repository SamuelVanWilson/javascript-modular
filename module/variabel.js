export {input, button, listTask};

const input = document.querySelector('input');
const button = document.querySelector('button[type="button"]');
const listTask = document.querySelector('.groupList>ul');